<section class="controls-block">
  <h2>#=(control_block.title)</h2>
  <div class="block-content">
    #_ control_block.controls.forEach(function(control) {
      <div class="control">
        #_ if (control.type == "tabs") {
          <label>#=(control.name)</label>
          #_ const tabs = control;
          #_ include ../common/tabs-unique-1.0.0.ejs
        #_ } else {
          <label for="#=(control.id)">#=(control.name)</label>
          #_ if (control.type == "checkbox") {
            #_ const checkbox = control;
            #_ include ../common/checkbox-1.0.0.ejs
          #_ } else if (control.type == "range") {
            #_ const range = control;
            #_ include ../common/range-1.0.0.ejs
          #_ } else if (control.type == "button") {
            #_ const button = control;
            #_ include button-1.0.0.ejs
          #_ } else {
            #_ throw Error("Unknown control type " + control.type);
          # }
        #_ }
      </div>
    #_ })
  </div>
</section>
#_ { let initialState = (checkbox.checked === true) ? "checked" : "";
    #_ if (checkbox.enabled === false) { initialState += " disabled"; }
    #_ if (checkbox.style === "line") {
        #_ include checkbox-line-1.0.0.ejs
    #_ } else {
        #_ include checkbox-full-1.0.0.ejs
    #_ }
#_ }